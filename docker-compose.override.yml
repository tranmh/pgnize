# Dev-only overrides: expose db + minio on localhost for native `go run` / CLI access.
services:
  db:
    ports:
      - "127.0.0.1:5432:5432"
  minio:
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
  ollama:
    ports:
      - "127.0.0.1:11434:11434"
